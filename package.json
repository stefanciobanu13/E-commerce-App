{
  "name": "e-commerce-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Full-stack e-commerce application with React frontend and Express/PostgreSQL backend",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run client\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
